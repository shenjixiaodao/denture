<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yuzi.denture.data.clinic.mapper.DentureOrderMapper" >

    <insert id="save" parameterType="com.yuzi.denture.domain.clinic.DentureOrder">
        INSERT INTO denture_order
        (
        `id`,
        `clinic_id`
        )
        VALUES
        (
        #{id},
        #{clinicId}
        );
    </insert>

</mapper>